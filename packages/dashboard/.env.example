# API backend URL — leave empty if API is served from the same origin (e.g. via proxy)
# Set to your deployed API URL when the frontend is hosted separately (e.g. Vercel)
# Example: VITE_API_URL=https://your-api-server.example.com
VITE_API_URL=
